#!/usr/bin/env bash
#
# Dev launcher for the Python backend (services/agent-engine).
#
# The backend is not implemented yet (Phase 1 placeholder). When it exists,
# this script will activate the virtual environment and start the FastAPI dev
# server (e.g. `uvicorn app.main:app --reload --host 127.0.0.1`).
#
set -euo pipefail

echo "[agent-engine] Backend not implemented yet."
echo "[agent-engine] See docs/ROADMAP.md (Phase 1/2) and services/agent-engine/README.md."
